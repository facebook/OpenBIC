# Only takes effect for the dual-core sysbuild (west build --sysbuild).
# Tells cpu0's SoC init code (soc/nxp/mcx/mcxn/soc.c) to set cpu1's boot
# address and release it out of reset - without this, cpu1 never starts
# even if a cpu1 image is flashed alongside it.
CONFIG_SECOND_CORE_MCUX=y
